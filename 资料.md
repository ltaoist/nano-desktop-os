Nano Desktop OS 把后端和前端收拢为一个整体的编程平台，通过**逻辑线程编程**的方式，保持直觉性的代码组织和内聚的应用模型，为软件的纵深开发提供可靠的、直接可用的通用设施。Nano Desktop OS 提供了"逻辑线程列表"控件管理逻辑线程，并可在浏览器中进入逻辑线程的"内容窗口"，为面向对象和互操作性提供轻量的、全新的实现方式。Nano Desktop OS 定义并实现了线程对象代理（Thread Objects Broker）组件技术，让操作系统设施和浏览器设施能够互动、结构化和对话，让你喜爱的python类和JavaScript类能够协同工作。Nano Desktop OS 已经实现了应用管理器、线程管理器、认证、TOB、通知总线、SSE 总线、前端框架等组件，你只需将你的应用程序移植和接入，即可体验互操作带来的爽快体验。

我们将逻辑线程编程视为一种编程范式。CORBA 打开了互操作性的领域，RPC 将异构的服务和系统连接起来，Nano Desktop OS 则展示了逻辑线程编程所遵循的完整的结构、逻辑和原则。基于多重架构经验、多种应用开发经验、多种领域技术栈，Nano Desktop OS 实现了逻辑线程编程的工程化落地，以极简的规约实现通用的前后端融合。Nano Desktop OS 带来封装的标准，带来 Python + JavaScript 的新的起点。

通过跨越多个层次和界面的封装和集成，逻辑线程编程已经在 Nano Desktop OS 验证实现，复杂的分析最后浓缩为几个原语的使用，你无需关注细节即可上手。

## 选择逻辑线程编程

Nano Desktop OS 通过逻辑线程编程组织软件的开发体验和使用体验，通过互操作建模，通过对象间的互动和协作描述并承载应用的功能，并可轻松通过 Python 和 JavaScript 实现新的应用程序，为健壮且一致的软件体验提供基石。

**轻松创建对象间的接口，适用于任何场景的逻辑线程概念**：逻辑线程是对应用的上下文的标识，它不是描述操作系统资源的概念，而是描述一个正在进行的会话。Nano Desktop OS 将生产力理解为多个并行的逻辑线程，每个逻辑线程都有自己的状态、逻辑和界面。充分利用浏览器 JavaScript 环境的交互与展示能力，以你熟悉的 Python 程序为基础，或将通过其他技术实现的服务和能力接入 Nano Desktop OS，打造你的个人工作环境，定义你的智能工作平台。

**兼顾真实场景及虚拟场景** Nano Desktop OS 只定义了服务器、后端、前端等概念，这是从交互的角度出发的，无论真实场景还是虚拟场景，都体现为逻辑线程的内容窗口的设计和更新。你既可以在内容窗口中监控数据，又可以在内容窗口中运行仿真，甚至启动你喜爱的游戏。Nano Desktop OS 应用管理器可以安装你的应用，你可以以脚本的方式也可以以文件夹的方式创建应用，并在线程启动器中开始一个新的逻辑线程。

逻辑线程编程让不同于微服务架构、进程间通信、事件驱动编程等前后端技术的应用开发成为可能，应用可通过 Python 后端便捷进行各类计算，调用各类接口，为前端提供更加丰富的数据和领域来源。使用者可以专注于对工作流和逻辑线程的设计，摆脱繁杂的云计算设施和绕来绕去的事件编程，保持软件体验的清爽。

**超越连接** 逻辑线程让应用程序的运行超越连接的边界，数据库、科学计算、人工智能、计算机仿真等技术都可以在逻辑线程中运行，实现数据和能力的整合和扩展。TOB 支持基于 JSON 的参数和返回值，这几乎胜任所有的场合。

**让数据在对象间流动，仅此而已** Nano Desktop OS 提供了基于应用名称的存储，每个应用可选择将数据存储在自己的目录中。我们提倡每个应用定义自己的个性化的数据和领域模型，鼓励垂直领域的应用的研发需要。垂直领域可能需要使用自己的特有的数据格式、协议和处理逻辑，通过 Nano Desktop OS 应用存储可以相当便捷地完成垂直领域的集成。这是一种类似于手机上的应用数据的体验，如此轻便和简单。

**线程正在运行** 通过 Nano Desktop OS 的线程管理器组织你的工作空间，在线程内容窗口中查看不同工作的最新状态，深度参与，及时推进。Nano Desktop OS 的线程内容窗口被定义为可重入的，你可以像一般的操作系统窗口那样在不同的窗口间切换。通过唯一的线程 ID，线程管理器为线程内容窗口提供了统一的上下文识别和恢复的机制。

**面向不断生成的体验** Nano Desktop OS 完整支持 Python 的线程库和 JavaScript Vue.js 的开发，你可以选择多种成熟的技术栈来实现你的应用。如果你需要进一步的帮助，也可以联系我们。

*Nano Desktop OS SDK 帮你快速创建 Nano OS 应用。通过 Nano Desktop OS 安装器，用户可以安装你开发的应用，浏览相关的内容，使用你精心定制的功能和服务。*

## 从线程对象代理到你的应用

线程对象代理（Thread Objects Broker，TOB）从设计之初就考虑到逻辑线程编程对低延迟和高可靠性的要求。线程对象代理是一种组件技术，它利用线程 ID 和命名服务提供强大的互操作体验，带来全新的应用开发体验。

Nano Desktop OS 提供了一个浏览器环境的工作界面，让应用界面看起来就像一个统一的操作系统环境，通过线程管理器和应用管理器来支持用户组织工作和数据。运行在浏览器环境的内容窗口是用户交互的载体，通过前后端的 TOB 交互，为用户看到的内容提供来源和为用户的操作提供丰富而有效的语义。用户在浏览应用时，内容窗口可以基于浏览器前端技术构建交互和展现内容，或者启动某种功能。

Nano Desktop OS 搭载线程列表控件，用户可以在不同的工作任务之间保持清晰的视野和上下文。当用户开启多个逻辑线程时，线程列表控件展示每个线程的运行状态和所属应用，让用户可以一眼掌握所有工作的进展。当用户在线程之间切换时，线程列表控件会高亮当前正在交互的逻辑线程，让用户知道正在浏览哪个任务。

## 线程对象代理类

Nano Desktop OS SDK 提供了 Python 版本和 JavaScript 版本的内涵统一而一致的线程对象代理类。这是一个简明的可编程对象，它让你能定义数据的产生和返回向何处取、从哪里来，实现应用体验的连贯和一致。

Nano Desktop OS 实现了线程对象代理服务器，不同的环境中的 TOB 自动向服务中心注册，由此不同的 TOB 可以相互协作。当用户在浏览器上操作应用时，浏览器环境的 TOB 可能正在调用 Python 后端的 TOB，用户的操作可以精确地映射到后端的功能，在清晰的逻辑中响应用户的操作。当用户开启多个线程、多个应用时，线程对象代理服务器可以根据线程 ID 自动处理不同线程的不同的 TOB，线程对象代理服务器已经定义了线程独占的网络通道用于驱动互操作。

## 基于严格的分布式原语

Nano Desktop OS 旨在提供可严格而准确地定制用户体验的机制。通过调用 TOB 函数（callTOBMethod）、挂载 TOB 函数（mountTOBMethod）这两个原语，Nano Desktop OS 支持不同环境中的执行点和计算能力的注册和调度，支持计算密集和交互密集的场景。这两个原语的使用灵活且可扩展，你可以精准定义应用的逻辑和流程。如果你需要在其他的平台或环境接入 Nano Desktop OS 实现互操作，同样可以参考现有实现完成。

TOB 命名与时间对齐也是一项有用的功能，通过为 TOB 命名（nameTOB）、等待命名 TOB（waitNamedTOB）、等待 TOB（waitTOB）等原语，你即可通过 TOB 服务器实现不同的进程和环境的统一编排。不同的平台都可以高效率地等待不同平台的 TOB 的生命周期开始，你无需担心因同步异常导致用户看到错误或怪异的状态。

除了调用、命名和发现，可初始化 TOB 机制（initializeTOBM）、关闭 TOB 机制（closeTOBM）、创建 TOB（createTOB）、注销 TOB（forgetTOB）同样易于使用且灵活，TOB 服务器已经实现所有繁琐的配置和复杂的基础架构。

得益于分布式理论的严谨，Nano Desktop OS 优先关注了单机、多进程、前后端互联的场景——基于线程对象代理服务器的高效分发，通过在环境变量和 URL 自动注入线程 ID，实现用户体验的一致和连贯。底层是基于哈希表和映射关系的对象搜索，兼顾极致性能和易用性。Nano Desktop OS 提供基本的、稳定的中心节点，即使多达数十个应用程序依然性能充足。

## 可扩展的安全模型和隐私模型

基于认证和登录，Nano Desktop OS 可以兼容不同的安全模型的方式产生数据和使用数据。

Nano Desktop OS 仅定义了基本的认证和登录，并不绑定某种特定的安全模型，你可以选择不同的认证模型。如果你不希望使用 Nano Desktop OS 的安全模型，你可以接入自己的认证机制和存储机制，实现更复杂的数据安全和隐私保护。

我们建议敏感的用户数据至少应当防窃取、防篡改、防重放攻击和防中间人攻击。对象代理服务器不会截留调用函数过程中的参数和返回值，也不需要配置云端服务器以实现数据的上传和下载。Nano Desktop OS 线程列表控件让用户对运行环境保持基本的感知，但它仅提供线程的基本信息而不涉及具体的数据内容。

### 使用MIT许可证

Nano Desktop OS 采用 **MIT** 许可证开源，现已可在代码仓库获取。开发者可至仓库了解、体验并定制 Nano Desktop OS。更多关于 Nano Desktop OS 的资讯，欢迎浏览项目 GitHub 仓库。
