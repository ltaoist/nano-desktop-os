import{a as e,c as t,i as n,l as r,n as i,o as a,r as o,s,t as c}from"./app-Duc5LDOZ.js";import"./calc-4GHXgrXv.js";import"./start_thread-CLffxJpH.js";import"./snake-CEMR77Kv.js";var l=JSON.parse(`{"path":"/en/guide/","title":"Usage Guide","lang":"en-US","frontmatter":{},"git":{},"filePathRelative":"en/guide/README.md"}`),u={name:`README.md`};function d(c,l,u,d,f,p){let m=t(`RouteLink`);return s(),o(`div`,null,[l[3]||=n(`<h1 id="usage-guide" tabindex="-1"><a class="header-anchor" href="#usage-guide"><span>Usage Guide</span></a></h1><h2 id="startup-and-login" tabindex="-1"><a class="header-anchor" href="#startup-and-login"><span>Startup and Login</span></a></h2><p>Run the following command in the project root to start the service:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">python src/backend/launch.py</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The terminal prints the frontend URL (default <code>http://localhost:5173</code>) and the default account (<code>admin</code> / <code>admin</code>). Open that URL in a browser.</p><p><img src="/nano-desktop-os/assets/system_launch.png" alt="System startup"></p><p>You will see the login screen:</p><p><img src="/nano-desktop-os/assets/login.png" alt="Login screen"></p><p>Enter the username and password. After authentication passes, you enter the main interface.</p><h2 id="main-interface-and-app-management" tabindex="-1"><a class="header-anchor" href="#main-interface-and-app-management"><span>Main Interface and App Management</span></a></h2><p>After login, the left side of the main interface is the function panel and the right side is the content window area. The three entries at the top of the left panel can be switched at any time:</p><ul><li><strong>Launcher</strong> (+): shows the list of installed apps. Clicking an app icon creates a thread and starts working. The system preinstalls two apps: calc.App (calculator) and snake.App (Snake).</li><li><strong>Installer</strong> (📦): install new apps (folder or ZIP), uninstall apps, or clear an app&#39;s persistent data.</li><li><strong>Notification Center</strong> (🔔): view system notifications, delete a single one, or clear all.</li></ul><p>Below the function buttons is the <strong>thread list</strong>, showing all running threads. Click a list item to switch between windows; switching does not interrupt background work. The bottom button collapses the panel for a larger workspace.</p><p><img src="/nano-desktop-os/assets/browse_apps.png" alt="Main interface"></p><p><img src="/nano-desktop-os/assets/start_thread.png" alt="Create a thread"></p><h2 id="threads-and-windows" tabindex="-1"><a class="header-anchor" href="#threads-and-windows"><span>Threads and Windows</span></a></h2><p>When you click an app in the launcher, the system creates a logical thread for that app: it starts the app&#39;s backend process, opens a content window to load the frontend page, and the thread appears in the left list. You can create multiple threads for the same app, each running independently.</p><p>Each thread window has a title bar at the top showing the thread name and a close button (×). Double-click the title bar to rename the thread. Clicking × closes the thread; the system terminates that thread&#39;s backend process, and temporary in-memory state is lost. Data already persisted to disk (such as the Snake leaderboard) is not affected by closing.</p><h2 id="built-in-examples" tabindex="-1"><a class="header-anchor" href="#built-in-examples"><span>Built-in Examples</span></a></h2><p>The system preinstalls two example apps. You can open them directly, and they also demonstrate two typical frontend-backend interaction patterns:</p><ul><li><strong>calc.App (calculator)</strong>: request-response pattern. The frontend sends an expression and the backend evaluates it and returns the result. Calculation history is kept in memory and is not retained after the thread closes.</li></ul><p><img src="/nano-desktop-os/assets/calc.png" alt="Calculator"></p><ul><li><strong>snake.App (Snake)</strong>: data persistence pattern. The game logic runs in the browser; after the game ends the user can submit a score to the backend leaderboard. The leaderboard uses a file lock to make concurrent writes from multiple threads safe, and data is persisted.</li></ul><p><img src="/nano-desktop-os/assets/snake.png" alt="Snake"></p><h2 id="stopping-the-service" tabindex="-1"><a class="header-anchor" href="#stopping-the-service"><span>Stopping the Service</span></a></h2><p>The server can keep running, and you can close the browser window at any time without affecting background work. To fully stop the service, press <strong>Ctrl+C</strong> in the terminal running launch.py. The system shuts down (terminates thread processes and releases the port). Do not stop the service while data is still being written to disk or before the port is released, or data loss may occur.</p><p>After stopping, persistent data (leaderboard, app installation state) is fully retained; in-memory state (calculation history, unsubmitted scores) is lost; all threads are destroyed and must be recreated on the next startup.</p><hr>`,28),i(`ul`,null,[i(`li`,null,[l[1]||=e(`Next: `,-1),a(m,{to:`/en/dev/`},{default:r(()=>[...l[0]||=[e(`App Development`,-1)]]),_:1}),l[2]||=e(` — learn to write Nano Desktop OS applications`,-1)])])])}var f=c(u,[[`render`,d]]);export{l as _pageData,f as default};