import{i as e,r as t,s as n,t as r}from"./app-Duc5LDOZ.js";var i=JSON.parse(`{"path":"/en/minimal/app-filesystem.html","title":"Apps and Data","lang":"en-US","frontmatter":{},"git":{},"filePathRelative":"en/minimal/app-filesystem.md"}`),a={name:`app-filesystem.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="apps-and-data" tabindex="-1"><a class="header-anchor" href="#apps-and-data"><span>Apps and Data</span></a></h1><h2 id="app-type-detection" tabindex="-1"><a class="header-anchor" href="#app-type-detection"><span>App type detection</span></a></h2><p>The app manager identifies two app types by directory-name suffix, in <code>get_app_type()</code> and <code>get_executive_file()</code>:</p><table><thead><tr><th>Suffix</th><th>Type</th><th>Entry file</th><th>Description</th></tr></thead><tbody><tr><td><code>.App</code></td><td><code>&quot;app&quot;</code></td><td><code>{name}.App/main.py</code></td><td>full app, includes a frontend page (index.html)</td></tr><tr><td><code>.py</code></td><td><code>&quot;script&quot;</code></td><td><code>{name}.py/{name}.py</code></td><td>script app, no separate frontend page</td></tr></tbody></table><p>The app list is generated by scanning <code>Data/AppData/</code> in real time on each <code>list_apps()</code> call; no persistent index is maintained. The functions related to <code>Data/System/apps_index.json</code> are defined but currently unused.</p><p><strong>icon field</strong>: the app icon is the capitalized first letter of the directory name (e.g. calc.App → &quot;C&quot;), a minimal icon scheme.</p><h2 id="app-content-serving" tabindex="-1"><a class="header-anchor" href="#app-content-serving"><span>App content serving</span></a></h2><p>The frontend of an <code>.App</code> app is served through three routes:</p><p><strong><code>GET /app-content/{app_name}</code></strong>: redirects to <code>/app-content/{app_name}/</code>, ensuring the browser resolves relative paths in directory form.</p><p><strong><code>GET /app-content/{app_name}/</code></strong>: the core route, which does one key thing — injects THREAD_ID:</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line"><span class="token keyword">with</span> <span class="token builtin">open</span><span class="token punctuation">(</span>index_html<span class="token punctuation">,</span> <span class="token string">&quot;r&quot;</span><span class="token punctuation">,</span> encoding<span class="token operator">=</span><span class="token string">&quot;utf-8&quot;</span><span class="token punctuation">)</span> <span class="token keyword">as</span> f<span class="token punctuation">:</span></span>
<span class="line">    html <span class="token operator">=</span> f<span class="token punctuation">.</span>read<span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">html <span class="token operator">=</span> html<span class="token punctuation">.</span>replace<span class="token punctuation">(</span><span class="token string">&quot;&lt;/head&gt;&quot;</span><span class="token punctuation">,</span> <span class="token string-interpolation"><span class="token string">f&#39;&lt;script&gt;window.THREAD_ID=&quot;</span><span class="token interpolation"><span class="token punctuation">{</span>thread_id<span class="token punctuation">}</span></span><span class="token string">&quot;;&lt;/script&gt;\\n&lt;/head&gt;&#39;</span></span><span class="token punctuation">)</span></span>
<span class="line"><span class="token keyword">return</span> HTMLResponse<span class="token punctuation">(</span>html<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>It reads the app directory&#39;s <code>index.html</code> and inserts a <code>&lt;script&gt;</code> that sets <code>window.THREAD_ID</code> before the <code>&lt;/head&gt;</code> closing tag. This is HTML-string-level injection that requires no cooperation from app code. <code>nano_tob.js</code> reads this global variable after it loads.</p><p><strong><code>GET /app-content/{app_name}/{path:path}</code></strong>: serves static resources (CSS, JS, images, etc.) from the app&#39;s <code>static/</code> subdirectory. Note that the static file path is <code>{app_dir}/static/{path}</code>, so app images and extra scripts must be placed under <code>static/</code>.</p><h3 id="where-nano-tob-js-is-served" tabindex="-1"><a class="header-anchor" href="#where-nano-tob-js-is-served"><span>Where nano_tob.js is served</span></a></h3><p>The <code>&lt;script src=&quot;/nano_tob.js&quot;&gt;</code> script is not in the backend routes; it is served by the frontend Vite dev server from <code>src/frontend/public/nano_tob.js</code>. In development mode (port 5173), the iframe&#39;s <code>/nano_tob.js</code> request is intercepted by Vite and returns the file in the public directory. After a production build, the frontend dist directory must be mounted to the backend (the current version relies on the Vite dev server).</p><h2 id="install-mechanism" tabindex="-1"><a class="header-anchor" href="#install-mechanism"><span>Install mechanism</span></a></h2><p><code>install_app(source_path)</code> supports three sources:</p><ol><li><strong>.zip file</strong>: <code>zipfile.ZipFile</code> extracts to <code>Data/AppData/{real_name}/</code>. The ZIP root must directly contain the entry file (no extra nested directory). Only zip is supported, not rar/tar.gz.</li><li><strong>Directory</strong>: <code>shutil.copytree()</code> copies the whole directory to <code>Data/AppData/</code>.</li><li><strong>Single file</strong>: creates a <code>Data/AppData/{filename}/</code> directory and copies the file in with <code>shutil.copy2()</code>.</li></ol><p>After installation it calls <code>get_executive_file()</code> to verify the entry file exists; on failure it deletes the copied directory and returns an error. <code>force_install_app()</code> deletes the same-name directory first and then installs, implementing overwrite installation.</p><h2 id="data-storage-framework" tabindex="-1"><a class="header-anchor" href="#data-storage-framework"><span>Data storage framework</span></a></h2><p><code>data_storage.py</code> implements app-private storage; the core design is <strong>filesystem-path storage isolated by app name</strong>:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Data/AppDataStore/{app_name}/</span>
<span class="line">└── _pathstore/           # path data storage root</span>
<span class="line">    ├── config.json       # /config.json → _pathstore/config.json</span>
<span class="line">    ├── leaderboard.json  # /leaderboard.json</span>
<span class="line">    └── subdir/           # /subdir/ → _pathstore/subdir/</span>
<span class="line">        └── data.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Paths start with <code>/</code>; <code>_sanitize_path()</code> removes the leading <code>/</code> and maps to a filesystem path under <code>_pathstore/</code>. <code>os.makedirs(os.path.dirname(target), exist_ok=True)</code> creates parent directories automatically.</p><p><strong>Path vs file</strong>: <code>listAppPathEntries</code> can list directory contents; <code>getAppPathData</code> returns None for directories and only reads files; <code>deleteAppPathData</code> recursively deletes directories (<code>shutil.rmtree</code>) and deletes files.</p><p><strong>API design principles</strong>:</p><ul><li>All synchronous functions — file I/O is fast enough in a desktop scenario, no async wrappers needed</li><li>No built-in locks — <code>data_storage</code> provides no cross-process locks; apps must handle concurrent writes themselves (like snake.App&#39;s file lock implementation)</li><li>No versioning — overwrite writes; apps manage their own data format migration</li><li><code>deleteAppDataStore(app_name)</code> deletes the entire app data directory, used by the &quot;clear data&quot; feature</li></ul><p><strong>System special app name</strong>: thread history data uses the app name <code>&quot;_system&quot;</code>, stored at <code>Data/AppDataStore/_system/thread_history/{thread_id}.json</code>, fully isolated from user app data.</p>`,27)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};