import{i as e,r as t,s as n,t as r}from"./app-BBH9_Ore.js";var i=JSON.parse(`{"path":"/minimal/app-filesystem.html","title":"应用与数据","lang":"en-US","frontmatter":{},"git":{"updatedTime":1786726823000,"contributors":[{"name":"Norman Mo","username":"","email":"ltaoist6@gmail.com","commits":1}],"changelog":[{"hash":"99910ee9697c4385f739104f0d98eddebdc0bc83","time":1786726823000,"email":"ltaoist6@gmail.com","author":"Norman Mo","message":"add docs"}]},"filePathRelative":"minimal/app-filesystem.md"}`),a={name:`app-filesystem.md`};function o(r,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[e(`<h1 id="应用与数据" tabindex="-1"><a class="header-anchor" href="#应用与数据"><span>应用与数据</span></a></h1><h2 id="应用类型识别" tabindex="-1"><a class="header-anchor" href="#应用类型识别"><span>应用类型识别</span></a></h2><p>应用管理器通过目录名后缀识别两种应用类型，逻辑在 <code>get_app_type()</code> 和 <code>get_executive_file()</code> 中：</p><table><thead><tr><th>后缀</th><th>类型</th><th>入口文件</th><th>说明</th></tr></thead><tbody><tr><td><code>.App</code></td><td><code>&quot;app&quot;</code></td><td><code>{name}.App/main.py</code></td><td>完整应用，包含前端页面（index.html）</td></tr><tr><td><code>.py</code></td><td><code>&quot;script&quot;</code></td><td><code>{name}.py/{name}.py</code></td><td>脚本应用，无独立前端页面</td></tr></tbody></table><p>应用列表通过每次调用 <code>list_apps()</code> 时实时扫描 <code>Data/AppData/</code> 目录生成，不维护持久化索引。<code>Data/System/apps_index.json</code> 相关函数已定义但当前未使用。</p><p><strong>icon 字段</strong>：应用图标取目录名首字母大写（如 calc.App → &quot;C&quot;），这是一个极简的图标方案。</p><h2 id="app-内容服务" tabindex="-1"><a class="header-anchor" href="#app-内容服务"><span>App 内容服务</span></a></h2><p><code>.App</code> 类型应用的前端通过三个路由服务：</p><p><strong><code>GET /app-content/{app_name}</code></strong>：重定向到 <code>/app-content/{app_name}/</code>，确保浏览器以目录形式解析相对路径。</p><p><strong><code>GET /app-content/{app_name}/</code></strong>：核心路由，做了一件关键的事——注入 THREAD_ID：</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py"><pre><code class="language-python"><span class="line"><span class="token keyword">with</span> <span class="token builtin">open</span><span class="token punctuation">(</span>index_html<span class="token punctuation">,</span> <span class="token string">&quot;r&quot;</span><span class="token punctuation">,</span> encoding<span class="token operator">=</span><span class="token string">&quot;utf-8&quot;</span><span class="token punctuation">)</span> <span class="token keyword">as</span> f<span class="token punctuation">:</span></span>
<span class="line">    html <span class="token operator">=</span> f<span class="token punctuation">.</span>read<span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">html <span class="token operator">=</span> html<span class="token punctuation">.</span>replace<span class="token punctuation">(</span><span class="token string">&quot;&lt;/head&gt;&quot;</span><span class="token punctuation">,</span> <span class="token string-interpolation"><span class="token string">f&#39;&lt;script&gt;window.THREAD_ID=&quot;</span><span class="token interpolation"><span class="token punctuation">{</span>thread_id<span class="token punctuation">}</span></span><span class="token string">&quot;;&lt;/script&gt;\\n&lt;/head&gt;&#39;</span></span><span class="token punctuation">)</span></span>
<span class="line"><span class="token keyword">return</span> HTMLResponse<span class="token punctuation">(</span>html<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>读取应用目录下的 <code>index.html</code>，在 <code>&lt;/head&gt;</code> 闭合标签前插入一段 <code>&lt;script&gt;</code> 设置 <code>window.THREAD_ID</code>。这是一种 HTML 字符串级别的注入，不需要应用代码做任何配合。<code>nano_tob.js</code> 加载后读取此全局变量。</p><p><strong><code>GET /app-content/{app_name}/{path:path}</code></strong>：从应用的 <code>static/</code> 子目录服务静态资源（CSS、JS、图片等）。注意静态文件路径是 <code>{app_dir}/static/{path}</code>，应用的图片、额外脚本等资源需要放在 <code>static/</code> 目录下。</p><h3 id="nano-tob-js-的服务位置" tabindex="-1"><a class="header-anchor" href="#nano-tob-js-的服务位置"><span>nano_tob.js 的服务位置</span></a></h3><p><code>&lt;script src=&quot;/nano_tob.js&quot;&gt;</code> 这个脚本不在后端路由中，而是由前端 Vite 开发服务器从 <code>src/frontend/public/nano_tob.js</code> 提供。在开发模式下（端口 5173），iframe 的 <code>/nano_tob.js</code> 请求被 Vite 拦截并返回 public 目录中的文件。在生产构建后，需要将前端 dist 目录挂载到后端（当前版本依赖 Vite dev server）。</p><h2 id="安装机制" tabindex="-1"><a class="header-anchor" href="#安装机制"><span>安装机制</span></a></h2><p><code>install_app(source_path)</code> 支持三种来源：</p><ol><li><strong>.zip 文件</strong>：<code>zipfile.ZipFile</code> 解压到 <code>Data/AppData/{real_name}/</code>。要求 ZIP 包根目录直接包含入口文件（不多套一层目录）。仅支持 zip 格式，不支持 rar/tar.gz。</li><li><strong>目录</strong>：<code>shutil.copytree()</code> 复制整个目录到 <code>Data/AppData/</code>。</li><li><strong>单文件</strong>：创建 <code>Data/AppData/{filename}/</code> 目录，<code>shutil.copy2()</code> 复制文件进去。</li></ol><p>安装后调用 <code>get_executive_file()</code> 验证入口文件存在，验证失败则删除已复制的目录并返回错误。<code>force_install_app()</code> 先删除同名目录再安装，实现覆盖安装。</p><h2 id="数据存储框架" tabindex="-1"><a class="header-anchor" href="#数据存储框架"><span>数据存储框架</span></a></h2><p><code>data_storage.py</code> 实现应用私有存储，核心设计是<strong>按应用名隔离的文件系统路径存储</strong>：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Data/AppDataStore/{app_name}/</span>
<span class="line">└── _pathstore/           # 路径数据存储根</span>
<span class="line">    ├── config.json       # /config.json → _pathstore/config.json</span>
<span class="line">    ├── leaderboard.json  # /leaderboard.json</span>
<span class="line">    └── subdir/           # /subdir/ → _pathstore/subdir/</span>
<span class="line">        └── data.txt</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>路径以 <code>/</code> 开头，<code>_sanitize_path()</code> 去掉前导 <code>/</code> 后映射到 <code>_pathstore/</code> 下的文件系统路径。<code>os.makedirs(os.path.dirname(target), exist_ok=True)</code> 自动创建父目录。</p><p><strong>路径 vs 文件</strong>：<code>listAppPathEntries</code> 可列出目录内容；<code>getAppPathData</code> 对目录返回 None 只读取文件；<code>deleteAppPathData</code> 对目录递归删除（<code>shutil.rmtree</code>）对文件删除文件。</p><p><strong>API 设计原则</strong>：</p><ul><li>全部同步函数——文件 I/O 在桌面场景下足够快，不需要异步封装</li><li>无内置锁——<code>data_storage</code> 不提供跨进程锁，应用需要自行处理并发写入（如 snake.App 的文件锁实现）</li><li>无版本管理——覆盖式写入，应用自行管理数据格式迁移</li><li><code>deleteAppDataStore(app_name)</code> 删除整个应用数据目录，用于&quot;清空数据&quot;功能</li></ul><p><strong>系统特殊应用名</strong>：线程历史数据使用应用名 <code>&quot;_system&quot;</code> 存储在 <code>Data/AppDataStore/_system/thread_history/{thread_id}.json</code>，与用户应用数据完全隔离。</p>`,27)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};